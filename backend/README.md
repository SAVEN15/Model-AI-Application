# Local Backend

Runs the local API, SQLite database, local upload store, Chroma vector store, and AI agent.

```powershell
python backend/main.py
```

Health check:

```text
http://127.0.0.1:8000/api/health
```
